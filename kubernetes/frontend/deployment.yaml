apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  namespace: property-app
  labels:
    app: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: react-frontend
          image: blagojaovezoski/property-frontend:96a1dc5
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: property-config
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
